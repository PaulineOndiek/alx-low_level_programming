#include "main.h"
/**
* _abs - computes the absolute value of an intetger
* @n: The number to be computed
* Return: absolute value of number or zero
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
