#include "main.h"
/**
* _isupper - checks if input is uppercase or not
*@c:the input
* Return: 1 if uppercase 0 otherwise
*/
int _isupper(int c)
{
for (c = 65; c <= 90; c++)
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
